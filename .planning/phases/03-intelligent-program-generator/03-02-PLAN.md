---
phase: 03-intelligent-program-generator
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/lib/__tests__/program-generator.test.ts
autonomous: true
requirements: [PROG-03]

must_haves:
  truths:
    - "Calm temperament produces skridt-first gait ordering after entry"
    - "Energetic temperament produces trav/galop-first ordering with skridt as mid-program pause"
    - "Neutral temperament produces trav-first then skridt then galop"
  artifacts:
    - path: "src/lib/__tests__/program-generator.test.ts"
      provides: "Temperament-specific test cases"
      contains: "temperament"
  key_links:
    - from: "src/lib/__tests__/program-generator.test.ts"
      to: "src/lib/program-generator.ts"
      via: "import generateProgramOrder"
      pattern: "import.*generateProgramOrder"
---

<objective>
Add dedicated temperament-strategy tests and verify the generator handles all three temperament modes correctly per CHOREOGRAPHY-RULES.md section 4.

Purpose: PROG-03 requires temperament to influence gait sequencing. Plan 01 implements the logic, but we need explicit tests proving each temperament mode (calm/energetic/neutral) produces the correct gait ordering pattern.

Output: Extended test file with temperament-specific test cases that all pass.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-intelligent-program-generator/03-CONTEXT.md
@.planning/phases/03-intelligent-program-generator/03-01-SUMMARY.md

@src/lib/program-generator.ts
@src/lib/__tests__/program-generator.test.ts
@src/data/kur-levels.ts

<interfaces>
From 03-CONTEXT.md decisions:
```
D-10: Calm horse: skridt early -> trav -> galop (gradual warm-up)
D-11: Energetic horse: trav -> galop early -> skridt as mid-program pause -> remaining galop
D-12: Neutral horse: trav -> skridt -> galop (balanced)
D-13: Passage/piaffe (when present at higher levels) always in climax section
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add temperament gait-ordering tests and verify they pass</name>
  <files>src/lib/__tests__/program-generator.test.ts</files>
  <read_first>
    - src/lib/__tests__/program-generator.test.ts (existing tests from Plan 01)
    - src/lib/program-generator.ts (implementation from Plan 01)
    - src/data/kur-levels.ts (LA level exercises with gaits: skridt id=1, trav id=2-7, galop id=8-13, overgang id=14)
    - .planning/phases/03-intelligent-program-generator/03-CONTEXT.md (decisions D-10 through D-13)
  </read_first>
  <behavior>
    **Temperament tests (D-10, D-11, D-12):**

    - Test "calm: first non-entry gait encountered is skridt":
      Call with temperament="calm", seed=42, LA level, empty ratings.
      After entry (index 0), the first exercise should have gait "skridt".
      The gait sequence overall should trend: skridt -> trav -> galop.

    - Test "energetic: first non-entry gait is trav or galop (not skridt)":
      Call with temperament="energetic", seed=42, LA level, empty ratings.
      After entry (index 0), the first exercise should have gait "trav" or "galop" (NOT "skridt").
      Skridt exercises should appear in the middle third of the program.

    - Test "neutral: first non-entry gait is trav":
      Call with temperament="neutral", seed=42, LA level, empty ratings.
      After entry (index 0), the first exercise should have gait "trav".

    - Test "calm vs energetic produce different gait orderings":
      Call with same level/ratings but different temperament. The gait sequence should differ.
      Specifically: for calm, indexOf(first skridt) < indexOf(first trav).
      For energetic, indexOf(first trav) < indexOf(first skridt).
  </behavior>
  <action>
    Add a new `describe("temperament gait sequencing")` block to the existing test file.

    Use LA level (KUR_LEVELS[0]) as fixture. Use seed=42 for determinism. Use empty ratings `{}` to isolate temperament effect.

    Helper function to extract gait sequence:
    ```typescript
    function gaitSequence(program: Exercise[]): Gait[] {
      return program.map(e => e.gait);
    }
    ```

    Write 4 test cases:

    1. `it("calm: skridt appears before trav after entry")` — per D-10
       - Get program with temperament="calm"
       - Skip index 0 (entry)
       - Find first skridt index and first trav index in remaining
       - Assert skridt index < trav index

    2. `it("energetic: trav/galop appears before skridt after entry")` — per D-11
       - Get program with temperament="energetic"
       - Skip index 0 (entry)
       - Assert program[1].gait is "trav" or "galop" (not "skridt")
       - Find first skridt in program, assert it's in middle third (index > N/3)

    3. `it("neutral: trav appears first after entry")` — per D-12
       - Get program with temperament="neutral"
       - Assert program[1].gait is "trav"

    4. `it("different temperaments produce different gait ordering")` — validates PROG-03
       - Generate with calm, energetic, neutral (all seed=42, same level, same ratings)
       - Extract gait sequences
       - Assert at least 2 of the 3 sequences differ

    After writing tests, run them. They should pass if Plan 01 implementation is correct. If any fail, investigate whether the test expectation or the implementation needs adjustment, and fix accordingly. The goal is GREEN for all tests.
  </action>
  <verify>
    <automated>cd C:/Users/ander/freestylemaker && npx vitest run src/lib/__tests__/program-generator.test.ts 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/__tests__/program-generator.test.ts contains `describe("temperament` (case insensitive)
    - File contains at least 4 new test cases with `temperament` in the test name
    - File contains `temperament: "calm"` in at least one test
    - File contains `temperament: "energetic"` in at least one test
    - File contains `temperament: "neutral"` in at least one test
    - All tests pass (exit code 0 from vitest run)
    - No regressions in existing tests
  </acceptance_criteria>
  <done>Temperament gait-ordering tests exist and pass, proving calm/energetic/neutral each produce distinct gait sequencing patterns per PROG-03 and decisions D-10 through D-12.</done>
</task>

</tasks>

<verification>
1. `npx vitest run` — all tests pass (generator + store tests)
2. `npx tsc --noEmit` — no TypeScript errors
</verification>

<success_criteria>
- Temperament-specific tests prove PROG-03 is satisfied
- Calm produces skridt-first sequencing
- Energetic produces trav/galop-first with skridt as mid-pause
- Neutral produces trav-first balanced ordering
- All tests pass with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/03-intelligent-program-generator/03-02-SUMMARY.md`
</output>
