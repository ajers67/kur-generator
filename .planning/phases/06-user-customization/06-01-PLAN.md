---
phase: 06-user-customization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/types/project.ts
  - src/lib/stores/wizard-store.ts
  - src/app/page.tsx
  - src/components/ProgramPreview.tsx
autonomous: true
requirements: [EDIT-01, EDIT-03, EDIT-04]

must_haves:
  truths:
    - "User can drag an exercise row up or down to reorder the program list"
    - "Dragged item is visually semi-transparent during drag, drop target shows blue border"
    - "After drop, the reordered program persists across page refresh via wizard-store"
    - "Validation re-runs automatically after reorder showing updated errors/warnings"
    - "Routes auto-regenerate when program order changes (existing useMemo dependency)"
    - "EDIT-03 regeneration button is NOT present (removed per business model)"
  artifacts:
    - path: "src/lib/types/project.ts"
      provides: "customProgramOrder field on ProjectData"
      contains: "customProgramOrder"
    - path: "src/lib/stores/wizard-store.ts"
      provides: "setCustomProgramOrder action and customProgramOrder state"
      contains: "customProgramOrder"
    - path: "src/components/ProgramPreview.tsx"
      provides: "Drag-and-drop reordering of exercise rows"
      contains: "draggable"
    - path: "src/app/page.tsx"
      provides: "Custom order override of computed program order"
      contains: "customProgramOrder"
  key_links:
    - from: "src/components/ProgramPreview.tsx"
      to: "src/lib/stores/wizard-store.ts"
      via: "onReorder callback calling setCustomProgramOrder"
      pattern: "setCustomProgramOrder"
    - from: "src/app/page.tsx"
      to: "src/lib/stores/wizard-store.ts"
      via: "customProgramOrder state selector"
      pattern: "customProgramOrder.*\\?.*computedProgramOrder"
---

<objective>
Add drag-and-drop exercise reordering to ProgramPreview and wire the custom order through the state system so changes persist, routes regenerate, and validation re-runs automatically.

Purpose: EDIT-01 (reordering), EDIT-03 (explicitly removed), EDIT-04 (real-time sync between program list and arena view)
Output: ProgramPreview with drag handles, customProgramOrder in store, page.tsx respecting custom order
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-user-customization/06-CONTEXT.md

@src/lib/types/project.ts
@src/lib/stores/wizard-store.ts
@src/app/page.tsx
@src/components/ProgramPreview.tsx

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/lib/types/project.ts:
```typescript
export interface ProjectData {
  selectedLevel: KurLevel | null;
  horseName: string;
  temperament: "calm" | "neutral" | "energetic";
  exerciseRatings: Record<number, StrengthRating>;
  programOrder: number[];
  arenaPaths: ArenaPath[];
  musicSettings: MusicSettings;
  currentStep: Step;
}
```

From src/lib/stores/wizard-store.ts:
```typescript
interface WizardState extends ProjectData {
  setProgramOrder: (order: number[]) => void;
  setArenaPaths: (paths: ArenaPath[]) => void;
  // ... other actions
}
```

From src/components/ProgramPreview.tsx:
```typescript
interface Props {
  level: KurLevel;
  ratings: Record<number, StrengthRating>;
  horseName: string;
  temperament: "calm" | "neutral" | "energetic";
  onBack: () => void;
  onNext?: () => void;
}
// Currently calls generateProgramOrder() internally on line 21
```

From src/app/page.tsx (lines 61-66):
```typescript
const computedProgramOrder = useMemo(
  () => selectedLevel
    ? generateProgramOrder(selectedLevel, exerciseRatings, temperament, { seed: programSeed })
    : [],
  [selectedLevel, exerciseRatings, temperament, programSeed]
);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add customProgramOrder to types, store, and page.tsx wiring</name>
  <files>src/lib/types/project.ts, src/lib/stores/wizard-store.ts, src/app/page.tsx</files>
  <read_first>src/lib/types/project.ts, src/lib/stores/wizard-store.ts, src/app/page.tsx</read_first>
  <action>
    **src/lib/types/project.ts** — Per D-19 to D-22:
    - Add `customProgramOrder: number[] | null` to `ProjectData` interface (null = use generated, array = use custom)
    - Add `customProgramOrder: null` to `DEFAULT_PROJECT_DATA`

    **src/lib/stores/wizard-store.ts** — Per D-04, D-19:
    - Add `setCustomProgramOrder: (order: number[] | null) => void` to WizardState interface
    - Implement `setCustomProgramOrder` action: `set({ customProgramOrder: order })`
    - Add `customProgramOrder` to the `loadProject` data spread and `saveCurrentProject` data object
    - In `setLevel` action, also reset `customProgramOrder: null` (changing level invalidates custom order)

    **src/app/page.tsx** — Per D-20, D-15:
    - Read `customProgramOrder` from wizard store: `const customProgramOrder = useWizardStore((s) => s.customProgramOrder)`
    - Read `setCustomProgramOrder` from wizard store
    - After `computedProgramOrder` useMemo (line 66), add: `const activeProgramOrder = useMemo(() => { if (customProgramOrder && selectedLevel) { return customProgramOrder.map(id => selectedLevel.exercises.find(e => e.id === id)).filter(Boolean) as Exercise[]; } return computedProgramOrder; }, [customProgramOrder, computedProgramOrder, selectedLevel]);`
    - Replace all uses of `computedProgramOrder` with `activeProgramOrder` in the JSX (ArenaRouteView programOrder prop, MusicManager programOrder prop)
    - Pass `programOrder={activeProgramOrder}` and `onReorder={setCustomProgramOrder}` as new props to ProgramPreview (instead of having ProgramPreview call generateProgramOrder internally)
    - Keep the existing `computedProgramOrder` -> `setProgramOrder` sync useEffect, but change it to sync `activeProgramOrder` instead
    - EDIT-03 REMOVED: Do NOT add any "Generer nyt program" or regeneration button. Per D-11, regeneration is removed from scope (business model: one program per purchase).
  </action>
  <verify>
    <automated>cd C:/Users/ander/freestylemaker && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "customProgramOrder" src/lib/types/project.ts
    - grep -q "setCustomProgramOrder" src/lib/stores/wizard-store.ts
    - grep -q "customProgramOrder" src/app/page.tsx
    - grep -q "activeProgramOrder" src/app/page.tsx
  </acceptance_criteria>
  <done>customProgramOrder field exists in ProjectData, store has setter action, page.tsx resolves active order (custom if set, computed if null), TypeScript compiles cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Add HTML5 drag-and-drop to ProgramPreview exercise rows</name>
  <files>src/components/ProgramPreview.tsx</files>
  <read_first>src/components/ProgramPreview.tsx, src/lib/types/project.ts</read_first>
  <action>
    Refactor ProgramPreview to accept program order as props instead of computing internally. Add HTML5 drag-and-drop per D-01 through D-05.

    **Props change:**
    - Add `programOrder: Exercise[]` prop (the active exercise list from page.tsx)
    - Add `onReorder: (order: number[] | null) => void` prop (calls setCustomProgramOrder)
    - Remove the internal `generateProgramOrder()` call on line 21
    - Use `programOrder` prop directly instead of the local `program` variable

    **Drag-and-drop implementation (per D-01, D-02, D-03):**
    - Add `const [dragIndex, setDragIndex] = useState<number | null>(null)` for tracking drag source
    - Add `const [dropIndex, setDropIndex] = useState<number | null>(null)` for tracking drop target
    - On each exercise row div:
      - `draggable={true}` (but NOT on entry/first or finale/last items — per D-05, entry always first, finale always last)
      - `onDragStart`: set `dragIndex` to current index, set `e.dataTransfer.effectAllowed = "move"`
      - `onDragOver`: `e.preventDefault()`, set `dropIndex` to current index
      - `onDragLeave`: clear `dropIndex` if leaving this item
      - `onDrop`: reorder the array (move item from dragIndex to dropIndex), call `onReorder(newOrder.map(e => e.id))`, clear both indices
      - `onDragEnd`: clear dragIndex and dropIndex
    - Per D-02: Add a grip icon (Unicode "&#x2630;" or SVG 6-dot grip) on the left side of each draggable row, before the sequence number. Non-draggable rows (entry/finale) show no grip.
    - Per D-03: Style the dragged item with `opacity: 0.4` when `dragIndex === i`. Style the drop target with `border-2 border-blue-400 bg-blue-50` when `dropIndex === i`.

    **Reorder logic:**
    - Create a helper `reorderExercises(list: Exercise[], fromIndex: number, toIndex: number): Exercise[]` that moves the item and returns new array
    - Ensure entry (index 0) and finale (last index) cannot be dragged or be drop targets
    - After reorder, call `onReorder(newOrder.map(e => e.id))` which sets customProgramOrder in the store

    **Per D-05:** Validation already re-runs via existing useMemo on `[level, programOrder]` — no additional wiring needed since programOrder prop changes trigger re-render.
  </action>
  <verify>
    <automated>cd C:/Users/ander/freestylemaker && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "draggable" src/components/ProgramPreview.tsx
    - grep -q "onDragStart" src/components/ProgramPreview.tsx
    - grep -q "onDrop" src/components/ProgramPreview.tsx
    - grep -q "onReorder" src/components/ProgramPreview.tsx
    - grep -q "programOrder" src/components/ProgramPreview.tsx (as prop, not internal generation)
  </acceptance_criteria>
  <done>Exercise rows are draggable with grip handles, visual feedback during drag (semi-transparent source, blue border target), drop reorders the list and persists via onReorder callback, entry/finale are locked in place, validation updates after reorder</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — TypeScript compiles with no errors
2. `npm run build` — Next.js build succeeds
3. Manual: Open app, go to Program step, drag an exercise row to a new position. Observe semi-transparent drag, blue drop target, updated order after drop. Navigate to Arena step and confirm routes match new order.
</verification>

<success_criteria>
- ProgramPreview accepts programOrder as prop (no internal generation)
- Exercise rows have drag handles and HTML5 DnD behavior
- Custom order persists in wizard-store via customProgramOrder field
- page.tsx uses custom order when set, falls back to computed order
- No regeneration button exists (EDIT-03 removed per D-11)
- Validation and routes update automatically after reorder
</success_criteria>

<output>
After completion, create `.planning/phases/06-user-customization/06-01-SUMMARY.md`
</output>
