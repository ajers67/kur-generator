---
phase: 05-auto-generated-arena-routes
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/components/ArenaRouteView.tsx
  - src/components/ArenaCanvas.tsx
  - src/app/page.tsx
autonomous: false
requirements: [ROUTE-04]

must_haves:
  truths:
    - "Arena step shows auto-generated routes color-coded by gait instead of freehand editor"
    - "Each route has an exercise label at its midpoint"
    - "Transition lines (gray dashed) connect the end of each route to the start of the next"
    - "Skridt routes are drawn dashed (existing pattern)"
    - "User can click 'Gener nye ruter' to regenerate with a new seed"
    - "Start dot and end arrow shown on each route"
  artifacts:
    - path: "src/components/ArenaRouteView.tsx"
      provides: "Read-only arena view with auto-generated routes, transition lines, labels, and regenerate button"
      min_lines: 60
    - path: "src/components/ArenaCanvas.tsx"
      provides: "Refactored to support both interactive and read-only modes, with label and transition rendering"
    - path: "src/app/page.tsx"
      provides: "Arena step uses ArenaRouteView instead of ArenaEditor"
  key_links:
    - from: "src/components/ArenaRouteView.tsx"
      to: "src/lib/route-generator.ts"
      via: "generateRoutes call"
      pattern: "import.*generateRoutes.*route-generator"
    - from: "src/components/ArenaRouteView.tsx"
      to: "src/components/ArenaCanvas.tsx"
      via: "ArenaCanvas rendering"
      pattern: "import.*ArenaCanvas"
    - from: "src/app/page.tsx"
      to: "src/components/ArenaRouteView.tsx"
      via: "Arena step renders ArenaRouteView"
      pattern: "import.*ArenaRouteView"
    - from: "src/components/ArenaRouteView.tsx"
      to: "src/lib/stores/wizard-store.ts"
      via: "setArenaPaths to persist generated routes"
      pattern: "setArenaPaths"
---

<objective>
Replace the freehand ArenaEditor with a read-only ArenaRouteView that displays auto-generated routes, transition lines, and exercise labels. Integrate into the wizard's arena step.

Purpose: Users see their program visualized in the arena automatically — no manual drawing required.
Output: Working arena visualization with color-coded routes, labels, transitions, and a regenerate button.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-auto-generated-arena-routes/05-CONTEXT.md
@.planning/phases/05-auto-generated-arena-routes/05-01-SUMMARY.md

@src/components/ArenaCanvas.tsx
@src/components/ArenaEditor.tsx
@src/app/page.tsx
@src/lib/types/project.ts
@src/lib/stores/wizard-store.ts
</context>

<interfaces>
<!-- From Plan 01 output (route-generator.ts) -->
```typescript
export interface ArenaRoute {
  exerciseId: number;
  exerciseName: string;
  gait: Gait;
  points: PathPoint[];
  rating: StrengthRating;
}

export function generateRoutes(
  programOrder: Exercise[],
  ratings: Record<number, StrengthRating>,
  options?: { seed?: number }
): ArenaRoute[];
```

<!-- From ArenaCanvas.tsx -->
```typescript
export interface PathPoint { x: number; y: number; }
export interface ArenaPath {
  points: PathPoint[];
  gait: Gait;
  exerciseId: number;
  exerciseName: string;
}
// ArenaCanvas Props include: paths, currentPath, currentGait, isDrawing, onMouseDown/Move/Up, sequenceNumber
```

<!-- From wizard-store.ts -->
```typescript
setArenaPaths: (paths: ArenaPath[]) => void;
```

<!-- From page.tsx arena step (lines 233-264) -->
```typescript
// Currently renders:
<ArenaEditor
  level={selectedLevel}
  ratings={exerciseRatings}
  programOrder={computedProgramOrder}
  onPathsChange={setArenaPaths}
/>
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Refactor ArenaCanvas for read-only mode and create ArenaRouteView</name>
  <files>src/components/ArenaCanvas.tsx, src/components/ArenaRouteView.tsx</files>
  <read_first>
    - src/components/ArenaCanvas.tsx (full current implementation — Props, drawPath, toCanvasCoords, draw function)
    - src/components/ArenaEditor.tsx (UI layout pattern — arena left, exercise list right, gait legend)
    - src/lib/route-generator.ts (Plan 01 output — generateRoutes, ArenaRoute type)
    - src/data/kur-levels.ts (GAIT_COLORS, GAIT_LABELS, Gait type)
    - .planning/phases/05-auto-generated-arena-routes/05-CONTEXT.md (D-13 through D-22)
  </read_first>
  <action>
    **Step A: Refactor ArenaCanvas to support read-only mode (per D-22)**

    Make all interactive props optional so ArenaCanvas can render without mouse handlers:
    - Change Props interface: make `currentPath`, `currentGait`, `isDrawing`, `onMouseDown`, `onMouseMove`, `onMouseUp` all optional with sensible defaults (empty array, "trav", false, noop functions)
    - Add optional props: `labels?: { exerciseId: number; label: string; point: PathPoint }[]` for exercise labels at route midpoints (per D-18)
    - Add optional prop: `transitions?: { from: PathPoint; to: PathPoint }[]` for gray dashed transition lines between routes (per D-13)
    - In the `draw` function, after drawing completed paths, draw transition lines: gray (#9ca3af) dashed lines, lineWidth 1, from each transition.from to transition.to
    - In the `draw` function, after drawing paths, draw labels: for each label, render the label text at the midpoint in a white rounded rect background with 9px font, fillStyle matching the gait color of the corresponding path
    - Keep all existing drawing logic (arena border, letters, center line, quarter lines, drawPath with start dot and end arrow, skridt dashed)
    - When no mouse handlers provided, render canvas without cursor-crosshair class and without pointer event handlers

    **Step B: Create ArenaRouteView component (per D-16)**

    New file `src/components/ArenaRouteView.tsx` ("use client"):
    - Props: `{ programOrder: Exercise[], ratings: Record<number, StrengthRating>, onRoutesGenerated: (paths: ArenaPath[]) => void }`
    - State: `seed` (number, initialized to `Date.now()`), `routes` (ArenaRoute[])
    - On mount and when programOrder/ratings/seed change: call `generateRoutes(programOrder, ratings, { seed })` and store result
    - Convert ArenaRoute[] to ArenaPath[] (drop the `rating` field) for ArenaCanvas and call `onRoutesGenerated` with them so they persist in wizard store
    - Compute `labels` array: for each route, calculate midpoint (points[Math.floor(points.length/2)]) and create label with short exercise name (truncate to ~15 chars if needed) plus sequence number
    - Compute `transitions` array (per D-13): for each consecutive pair of routes, create `{ from: routes[i].points[last], to: routes[i+1].points[0] }`
    - Render layout matching ArenaEditor pattern (per D-16 replacing it):
      - Left: ArenaCanvas in read-only mode (no mouse handlers) with paths, labels, transitions
      - Below canvas: gait legend (same pattern as ArenaEditor)
      - Below legend: "Gener nye ruter" button (per D-21) that sets seed to Date.now()
      - Right side: exercise list showing all exercises with sequence numbers, gait color dots, coefficient badges, and rating arrows (reuse the same list UI from ArenaEditor but without click-to-select interaction — just informational)

    Use existing Tailwind patterns from ArenaEditor for card styling, spacing, and responsive layout.
  </action>
  <verify>
    <automated>cd C:/Users/ander/freestylemaker && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "ArenaRouteView" src/components/ArenaRouteView.tsx
    - grep -q "generateRoutes" src/components/ArenaRouteView.tsx
    - grep -q "transitions" src/components/ArenaRouteView.tsx
    - grep -q "labels" src/components/ArenaRouteView.tsx
    - grep -q "Gener nye ruter" src/components/ArenaRouteView.tsx
    - grep -q "onRoutesGenerated" src/components/ArenaRouteView.tsx
    - grep -q "labels?" src/components/ArenaCanvas.tsx
    - grep -q "transitions?" src/components/ArenaCanvas.tsx
  </acceptance_criteria>
  <done>
    - ArenaCanvas accepts optional labels and transitions props, renders them correctly
    - ArenaCanvas works in read-only mode (no mouse handlers needed)
    - ArenaRouteView generates routes via generateRoutes(), displays them with labels and transitions
    - Gait legend and exercise list shown alongside the arena
    - "Gener nye ruter" button regenerates routes with new seed
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate ArenaRouteView into page.tsx arena step</name>
  <files>src/app/page.tsx</files>
  <read_first>
    - src/app/page.tsx (lines 1-50 for imports and state, lines 233-264 for arena step)
    - src/components/ArenaRouteView.tsx (Task 1 output — Props interface)
    - src/lib/stores/wizard-store.ts (setArenaPaths)
  </read_first>
  <action>
    1. Replace the ArenaEditor import with ArenaRouteView import:
       - Remove: `import { ArenaEditor } from "./ArenaEditor";` (if it exists as a direct import — check actual import style)
       - Add: `import { ArenaRouteView } from "@/components/ArenaRouteView";`

    2. In the arena step section (around line 233-264), replace:
       ```
       <ArenaEditor
         level={selectedLevel}
         ratings={exerciseRatings}
         programOrder={computedProgramOrder}
         onPathsChange={setArenaPaths}
       />
       ```
       with:
       ```
       <ArenaRouteView
         programOrder={computedProgramOrder}
         ratings={exerciseRatings}
         onRoutesGenerated={setArenaPaths}
       />
       ```

    3. Update the arena step header text (lines ~234-240):
       - Change heading from "Tegn dit program pa banen" to "Ruter i banen"
       - Change description from the freehand drawing instructions to: "Ruterne er automatisk genereret baseret pa ovelsestyper og styrker/svagheder. Klik 'Gener nye ruter' for et nyt forslag."

    4. Keep the Tilbage/Naeste buttons and navigation logic unchanged.

    5. Do NOT remove the ArenaEditor.tsx file — it may be useful reference for Phase 6 (user customization). Just stop importing it.
  </action>
  <verify>
    <automated>cd C:/Users/ander/freestylemaker && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "ArenaRouteView" src/app/page.tsx
    - grep -q "onRoutesGenerated" src/app/page.tsx
    - grep -q "Ruter i banen" src/app/page.tsx
    - grep -v "ArenaEditor" src/app/page.tsx | grep -q "ArenaRouteView" || true
  </acceptance_criteria>
  <done>
    - Arena step renders ArenaRouteView instead of ArenaEditor
    - Auto-generated routes appear when user reaches the arena step
    - Routes persist to wizard store via setArenaPaths/onRoutesGenerated
    - Build succeeds with no TypeScript errors
    - Header text reflects auto-generated routes (not freehand drawing)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of arena routes</name>
  <files>src/components/ArenaRouteView.tsx, src/components/ArenaCanvas.tsx, src/app/page.tsx</files>
  <action>
    Human visual verification of the complete arena route visualization. No code changes — verify that Tasks 1 and 2 produce the correct visual result.
  </action>
  <what-built>
    Auto-generated arena routes replacing the freehand drawing editor. Routes are color-coded by gait, connected with gray dashed transition lines, and labeled with exercise names. A "Gener nye ruter" button regenerates with a new random seed.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:3000
    2. Select any level (e.g. LA Kur), fill in horse profile and exercise ratings (mark some as strength, some weakness)
    3. Click through to the "Program" step, then advance to the "Bane" step
    4. Verify:
       a. Routes appear automatically on the arena canvas (no drawing needed)
       b. Routes are color-coded by gait (blue=trav, red=galop, black=skridt, etc.)
       c. Skridt routes are dashed
       d. Each route has a start dot and end marker
       e. Gray dashed lines connect the end of each route to the start of the next
       f. Exercise labels appear at route midpoints
       g. Strength-rated exercises appear more toward the C-end (top of arena)
       h. Weakness-rated exercises appear more toward the A-end (bottom) or sides
    5. Click "Gener nye ruter" and verify routes change
    6. Navigate away and back — routes should persist
  </how-to-verify>
  <verify>
    <automated>cd C:/Users/ander/freestylemaker && npx vitest run && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Visual confirmation from user that routes display correctly
  </acceptance_criteria>
  <done>
    - User confirms routes are visually correct and the arena step works as expected
  </done>
  <resume-signal>Type "approved" or describe any visual/functional issues</resume-signal>
</task>

</tasks>

<verification>
```bash
cd C:/Users/ander/freestylemaker && npx vitest run && npx next build 2>&1 | tail -10
```
All existing tests pass. Build succeeds. Arena step shows auto-generated routes.
</verification>

<success_criteria>
- ArenaRouteView replaces ArenaEditor in the wizard's arena step
- Routes are auto-generated, color-coded, labeled, and connected with transitions
- Strength/weakness zone placement is visually correct
- "Gener nye ruter" produces different route arrangements
- Routes persist to wizard store (survive page refresh)
- All existing tests continue to pass
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/05-auto-generated-arena-routes/05-02-SUMMARY.md`
</output>
